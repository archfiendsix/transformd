{
  "name": "transformd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:open": "npx cypress open",
    "cypress:headless": "npx cypress run --browser chrome --headless",
    "cypress:open-local": "npx cypress open --config-file ./cypress.local.config.js",
    "cypress:headless-local": "npx cypress run --browser chrome --headless --config-file ./cypress.local.config.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.17.1",
    "cypress-iframe": "^1.0.1",
    "cypress-mailslurp": "^1.7.0",
    "mailslurp-client": "^15.17.2"
  }
}
